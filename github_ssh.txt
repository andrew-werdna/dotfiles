#!/bin/bash

# must make this file executable and run it with `.`
# in order for it to work. Don't run it with `bash` command

# i.e. . ~/github_ssh.sh

eval $(ssh-agent -s)
wait

ssh-add path/to/private/key
